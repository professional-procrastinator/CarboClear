const { default: Layout } = require("@/components/Layout")

const Home = () => {
    return (
        <Layout>
            <div>
                <h1>Home</h1>
            </div>
        </Layout>
    )
}

export default Home;